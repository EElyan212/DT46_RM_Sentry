# 追击功能交接文档

## 1. 改动概述

本次改动将追击功能封装为可复用的 **Chase Action 服务**（`/tracker/chase`），其他节点调用即可触发追击，内部自动检查敌人跟踪状态（`tracked`）并执行导航。不再依赖 `rm_decision` 进行触发判断。

## 2. 文件变更清单

### 2.1 新增文件

| 文件路径 | 作用 |
|---|---|
| `src/rm_interfaces/action/Chase.action` | 追击 Action 定义：Goal(offset_distance)、Result(success/message)、Feedback(chasing/status) |
| `src/rm_interfaces/action/GetTargetPose.action` | 目标点计算 Action 定义：Goal(offset_distance)、Result(target_pose/success/message)、Feedback(enemy_x/enemy_y/enemy_tracked) |
| `src/rm_application/rm_application/target_pose_server.py` | 坐标计算服务节点：订阅 `/tracker/enemy_pose`，通过 tf2 查询 map 坐标，提供 GetTargetPose Action 服务 |
| `src/rm_application/rm_application/chase_client.py` | 追击服务节点：提供 Chase Action，内部检查 tracked 状态，调用 target_pose_server 获取目标点，通过 Nav2 执行导航 |
| `src/rm_application/scripts/test_chase.py` | 测试脚本：调用 Chase Action 触发追击 |
| `src/rm_application/launch/target_pose_server.launch.py` | target_pose_server 启动文件 |
| `src/rm_application/launch/chase_client.launch.py` | chase_client 启动文件 |
| `src/rm_application/launch/chase_system.launch.py` | 合并启动文件：同时启动 server + client |
| `src/rm_application/config/target_pose_server_params.yaml` | target_pose_server 参数配置 |
| `src/rm_application/config/chase_client_params.yaml` | chase_client 参数配置 |

### 2.2 修改文件

| 文件路径 | 修改内容 |
|---|---|
| `src/rm_interfaces/CMakeLists.txt` | 注册 `EnemyCenter.msg`、`GetTargetPose.action`、`Chase.action`，新增 `geometry_msgs`/`action_msgs` 依赖 |
| `src/rm_interfaces/package.xml` | 新增 `geometry_msgs`/`action_msgs` 依赖，删除重复的 `member_of_group` |
| `src/rm_application/setup.py` | 新增 `target_pose_server`、`chase_client`、`test_chase` 的 entry_point |
| `src/rm_application/rm_application/rm_decision.py` | 删除敌人数据订阅和追击触发逻辑，保留状态机和导航逻辑 |
| `src/rm_application/config/decision_params.yaml` | 删除 `chase_min_distance`/`chase_max_distance` 参数 |

## 3. 运行逻辑

### 3.1 整体通信链路

```
任意节点（rm_decision / 测试脚本 / 自定义节点）
    │
    │ Chase Action (/tracker/chase)
    ▼
chase_client (Action Server)
    │
    ├─ 订阅 /tracker/enemy_datas（检查 tracked 状态）
    │
    └─ GetTargetPose Action → target_pose_server → 返回目标点
           │
           └─ Nav2 导航执行
```

**视觉方数据链路**：
```
rm_tracker → /tracker/enemy_datas → rm_tf_broadcaster → tf + /tracker/enemy_pose → target_pose_server
```

### 3.2 各节点职责

| 节点 | 职责 |
|---|---|
| `rm_tf_broadcaster` | 订阅 `/tracker/enemy_datas`，发布 tf `camera→enemy` 和话题 `/tracker/enemy_pose` |
| `target_pose_server` | 订阅 `/tracker/enemy_pose`，通过 tf2 查询 `map→enemy` 获取 map 系坐标，计算偏移后的目标点，通过 GetTargetPose Action 返回 |
| `chase_client` | 提供 `/tracker/chase` Action 服务，订阅 `/tracker/enemy_datas` 检查 `tracked` 状态，调用 `target_pose_server` 获取目标点，通过 Nav2 导航 |
| `rm_decision` | 状态机逻辑（DEFENCE/OCCUPY/RETREAT 等），负责导航点管理和 Gimbal mode，不再负责追击触发 |

### 3.3 追击触发条件

```
其他节点调用 Chase Action (/tracker/chase, Goal: offset_distance)
  │
  ▼
chase_client 检查 tracked 状态（/tracker/enemy_datas）
  │
  ├─ tracked=true → 调用 target_pose_server 获取目标点 → Nav2 导航
  │
  └─ tracked=false → 返回 Result(success=false, message="敌人未被跟踪")
```

**触发方式**：任意节点创建 `ActionClient(Chase, '/tracker/chase')` 并发送 Goal 即可触发追击，无需关心 DEFENCE 状态或距离过滤。

**停止方式**：调用方取消 Goal（`goal_handle.cancel_goal_async()`）或 `chase_client` 检测到 `tracked=false` 时自动停止。

### 3.4 坐标转换逻辑

敌人坐标由视觉方发布在**相机坐标系**（z前、x右、y下），通过 tf2 自动转换到 map 坐标系：

```
tf2 树（自动组合）：
  map → odom → livox_frame → gimbal
                                ├── classis → base_footprint
                                └── muzzle → camera → enemy
                   │                                  │              │
                   │                                  │              └─ rm_tf_broadcaster 发布
                   │                                  └─ URDF 定义（robot_state_publisher）
                   └─ 定位系统发布（Point-LIO / FAST_LIO）
```

**注意**：实际帧名称为 `base_footprint`（非 `base_link`）、`enemy`（非 `enemy_link`）。

`target_pose_server` 通过一次 tf2 查询即可获取敌人 map 坐标：
```python
transform = tf_buffer.lookup_transform('map', 'enemy', ...)
enemy_map_x = transform.transform.translation.x
enemy_map_y = transform.transform.translation.y
```

无需手动计算旋转矩阵，tf2 自动完成相机→机体→世界的全部转换。

### 3.5 偏移计算逻辑

沿「机器人→敌人」方向，从敌人位置向后偏移 offset_distance 米：

```
enemy_map = tf2(map→enemy)           # 直接获取 map 系坐标
robot_pos = tf2(map→base_footprint)  # 机器人 map 系坐标

机器人(robot_pos) ──────── 目标点 ──── 敌人(enemy_map)
                               ↑
                        offset=1.5m（目标距敌人1.5m）

target = enemy_map - (enemy_map - robot_pos).normalized() × offset
```

## 4. 通信接口

### 4.1 话题

| 话题名 | 类型 | 消费者 | QoS | 说明 |
|---|---|---|---|---|
| `/tracker/enemy_datas` | `rm_interfaces/msg/EnemyCenter` | chase_client、rm_tf_broadcaster | BEST_EFFORT | 视觉方发布的敌人数据（相机坐标系） |
| `/tracker/enemy_pose` | `geometry_msgs/msg/PoseStamped` | target_pose_server | DEFAULT | rm_tf_broadcaster 转发的敌人位姿（相机坐标系） |

### 4.2 Action

#### 4.2.1 Chase: `/tracker/chase`

其他节点调用此 Action 触发追击：

```action
# Goal
float32 offset_distance    # 距敌人的偏移距离（米），<=0 时使用默认值
---
# Result
bool success               # 是否成功完成追击
string message             # 状态描述
---
# Feedback
bool chasing               # 是否正在追击中
string status              # 当前状态描述
```

#### 4.2.2 GetTargetPose: `/tracker/get_target_pose`

chase_client 内部调用此 Action 获取目标点（chase_client 自动调用，调用方无需关心）：

```action
# Goal
float32 offset_distance    # 距敌人的偏移距离（米），<=0 时使用默认值
---
# Result
geometry_msgs/PoseStamped target_pose    # 导航目标点（map 坐标系）
bool success                              # 是否成功
string message                            # 状态描述
---
# Feedback
float32 enemy_x         # 当前敌人世界坐标 x
float32 enemy_y         # 当前敌人世界坐标 y
bool enemy_tracked      # 敌人是否被跟踪
```

## 5. 参数配置

### 5.1 target_pose_server (`config/target_pose_server_params.yaml`)

```yaml
target_pose_server:
    ros__parameters:
        default_offset_distance: 0   # 默认偏移距离（米），Goal 中 offset<=0 时使用
```

### 5.2 chase_client (`config/chase_client_params.yaml`)

```yaml
chase_client:
    ros__parameters:
        offset_distance: 1.5        # 距敌人的偏移距离（米）
        chase_rate: 0.5             # 追击刷新频率（Hz）
        enemy_pose_timeout: 0.5     # 敌人数据超时阈值（秒），超过未收到视为丢失
```

## 6. 启动方式

### 6.1 合并启动（推荐）

```bash
ros2 launch rm_application chase_system.launch.py
```

### 6.2 单独启动

```bash
# 终端 1
ros2 run rm_application target_pose_server

# 终端 2
ros2 run rm_application chase_client
```

## 7. 测试方法

### 前置条件

需先启动以下节点（确保 TF 树完整）：
- `robot_state_publisher`（发布 URDF 固定变换）
- 定位系统（Point-LIO / FAST_LIO，发布 `map→odom`）
- `odo.cpp`（发布 `odom→base_footprint`）
- `rm_tf_broadcaster`（发布 `camera→enemy` 和 `/tracker/enemy_pose`）

### 测试步骤

```bash
# 终端 1：启动追击系统
ros2 launch rm_application chase_system.launch.py

# 终端 2：发布模拟敌人数据（tracked=true）
ros2 topic pub /tracker/enemy_datas rm_interfaces/msg/EnemyCenter \
  "{header: {stamp: {sec: 0, nanosec: 0}, frame_id: 'tracking_frame'}, \
    yaw: 0.0, x: 0.0, y: 0.0, z: 3.0, enemy_yaw: 0.0, armor_id: 0, tracked: true}" \
  --rate 10

# 终端 3：运行测试脚本触发追击
ros2 action send_goal /tracker/chase rm_interfaces/action/Chase "{offset_distance: 1.5}"

# 或带偏移参数
ros2 run rm_application test_chase -- --offset 2.0

# 验证 Action 列表
ros2 action list
ros2 action info /tracker/chase

# 验证 tf2 查询
ros2 run tf2_ros tf2_echo map enemy
```

### 其他节点调用示例

```python
from rm_interfaces.action import Chase
from rclpy.action import ActionClient

class SomeNode(Node):
    def __init__(self):
        self.chase_client = ActionClient(self, Chase, '/tracker/chase')

    def trigger_chase(self):
        goal = Chase.Goal()
        goal.offset_distance = 1.5
        future = self.chase_client.send_goal_async(goal)
        future.add_done_callback(self.on_chase_started)

    def on_chase_started(self, future):
        goal_handle = future.result()
        if goal_handle.accepted:
            result_future = goal_handle.get_result_async()
            result_future.add_done_callback(self.on_chase_done)

    def on_chase_done(self, future):
        result = future.result().result
        if result.success:
            self.get_logger().info(f'追击成功: {result.message}')
        else:
            self.get_logger().warn(f'追击失败: {result.message}')
```

## 8. 已知问题与注意事项

1. **rclpy.sleep 不存在**：ROS 2 Humble 中 `rclpy.sleep()` 不可用，已改为 `time.sleep()`

2. **TF 依赖**：坐标转换完全依赖 tf2 树，需要以下节点正常运行：
   - `robot_state_publisher`（发布 `base_footprint→classis→gimbal→muzzle→camera`）
   - 定位系统（发布 `map→odom`）
   - `odo.cpp`（发布 `odom→base_footprint`）
   - `rm_tf_broadcaster`（发布 `camera→enemy`）

3. **offset 语义**：偏移距离是**从敌人位置沿机器人→敌人方向向后退的距离**，不是机器人到目标点的直线距离。

4. **超时机制**：`chase_client` 通过 `/tracker/enemy_datas` 消息超时（`enemy_pose_timeout`，默认 0.5s）判断敌人是否被跟踪。超时未收到数据视为丢失，自动停止追击并返回失败。

5. **chase_client 是可复用服务**：其他节点通过调用 `/tracker/chase` Action 即可触发追击，无需关心内部实现。`rm_decision` 不再负责追击触发。

6. **enemy 坐标系**：`/tracker/enemy_pose` 和 tf `enemy` 均为**相机坐标系**（z前、x右、y下），非世界坐标系。server 通过 tf2 自动完成到 map 坐标系的转换。

7. **QoS 匹配**：`/tracker/enemy_datas` 必须用 `BEST_EFFORT`，否则收不到视觉方的消息。

